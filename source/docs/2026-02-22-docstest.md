---
date: 2026-02-22
title: "docstest"
draft: false
description: ""

# ファイル名に留意。以下を遵守。
#   {date}-{slug}.md
# title 要素を各所で使っているので必ず設定すること
#   render-heading.html にてpage kind（記事ページ）の最初のH1だけスキップ
#   ホームやセクションでは使われるので title 要素と同じものを設定すること
#   単独Markdownとして見たときの論理性を確保
# HUGOでのファイル名生成に date 情報を使っているので必須
# 添付ファイルは以下を遵守
#   {date}-{md name}--{img name}.{ext}
#   ハイフン2つを区切り文字として認識している
# description については現在は利用していない
---

# docstest

do-naru?